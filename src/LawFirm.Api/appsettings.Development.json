{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "//": "Local dev uses an in-memory database so no SQL Server is required. Authentication is not wired up yet, so endpoints are open.",
  "Database": {
    "Provider": "InMemory"
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:5173", "http://localhost:3000" ]
  }
}
